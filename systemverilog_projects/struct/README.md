# Struct - SystemVerilog

This folder covers the `struct` - a group of related variables kept together under one name.

## Files

| File | Description |
|---|---|
| `struct.sv` | A structure holding a `string`, an `int` and a `byte`, assigned with `'{}` and printed with `%p`. |

## Run

```bash
qverilog struct.sv
```
