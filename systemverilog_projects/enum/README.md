# Enum - SystemVerilog

This folder covers the enumerated (`enum`) data type and its navigation methods.

## Files

| File | Description |
|---|---|
| `enum.sv` | An `enum` listing the twelve months. The `first()` and `next()` methods are used to walk through the members one after another. |

## Run

```bash
qverilog enum.sv
```
